# 사용자 검증 체크리스트

대상 버전: **v0.1.2**
대상 기능: Google Vertex AI의 사용자 지정 Gemini 모델 ID 등록·선택·복원과 API Connections 모델 관리 팝업

이 문서는 자동 테스트만으로 확인할 수 없는 실제 SillyTavern·Vertex AI 환경을 사용자가 검증하기 위한 체크리스트입니다. 저장소의 체크박스를 직접 바꾸기보다는 이 문서를 복사해 개인 메모나 GitHub Issue에서 사용해 주세요.

## 상태 기록 방법

- `[ ]`: 아직 확인하지 않음
- `[x]`: 기대 결과와 동일하게 통과
- `실패`: 항목 ID와 실제 결과를 문제 보고 양식에 기록
- `차단`: 계정 권한, 모델 제공 리전, 테스트 환경 등의 이유와 해제 조건을 기록
- `해당 없음`: 사용하지 않는 인증 방식이나 기능이라 확인 대상이 아님

`필수` 항목은 v0.1 안정화 판단에 사용합니다. `조건부` 항목은 해당 인증 방식이나 기능을 사용하는 경우 필수입니다. `권장`과 `선택` 항목은 가능한 환경에서 확인해 주세요.

```text
진행 상태: 시작 전 / 진행 중 / 완료 / 차단
필수 통과: __ / __
조건부 통과: __ / __
현재 확인 중인 항목 ID:
현재 차단 사항:
마지막 확인일:
```

## 테스트 환경 기록

```text
테스트 날짜:
SillyTavern 버전:
Custom Model Router 버전:
운영체제 / 브라우저:
Vertex 인증 방식: Express / Full(Service Account)
테스트 리전:
테스트 모델 ID:
Connection Profile 사용 여부:
설치 전 기본 모델:
전체 결과: 통과 / 부분 통과 / 실패
```

API 키, Service Account JSON, 액세스 토큰, 개인 키는 이 문서나 GitHub Issue에 기록하지 마세요.

## 1. 시험 전 준비

- [ ] **[필수][PRE-01] SillyTavern 버전이 1.18.0 이상이다.**
  - 기대 결과: 버전 조건을 충족한다.
  - 결과/메모:
- [ ] **[필수][PRE-02] 확장 설치 전 기본 Vertex 모델로 메시지 생성이 성공한다.**
  - 기대 결과: 기존 인증, 프로젝트, 리전 설정이 정상임을 먼저 확인한다.
  - 결과/메모:
- [ ] **[필수][PRE-03] 민감정보를 제외한 현재 연결 방식·리전·기본 모델을 기록했다.**
  - 기대 결과: 문제가 생겨도 기존 연결 상태로 복구하고 설치 전후를 비교할 수 있다.
  - 결과/메모:
- [ ] **[조건부][PRE-03A] Connection Profile을 사용한다면 시험 대상 프로필을 백업했다.**
  - 기대 결과: 프로필 전환 시험 뒤 기존 상태로 복구할 수 있다.
  - 결과/메모:
- [ ] **[필수][PRE-04] 시험 모델의 정확한 공식 ID, 프로젝트 권한, 지원 리전을 확인했다.**
  - 기대 결과: 모델 ID 오타나 권한 문제를 확장 문제와 구분할 수 있다.
  - 결과/메모:
- [ ] **[필수][PRE-05] 시험 모델이 현재 SillyTavern 기본 Vertex 모델 목록에 없다.**
  - 기대 결과: 기본 모델 중복 검사가 아니라 실제 사용자 옵션 주입 경로를 시험할 수 있다.
  - 결과/메모:

## 2. 설치와 화면 표시

- [ ] **[필수][INS-01] Git 저장소 URL로 확장을 설치하고 SillyTavern을 새로고침했다.**
  - 설치 URL: `https://github.com/p16481012/Custom-Model-Router`
  - 기대 결과: 설치 오류 없이 확장이 활성화된다.
  - 결과/메모:
- [ ] **[필수][INS-02] API Connections의 Connection Profile 도구행에 사용자 모델 관리 아이콘이 표시된다.**
  - 기대 결과: 아이콘이 한 번만 표시되고 Vertex 연결 UI 및 다른 도구를 가리지 않는다.
  - 결과/메모:
- [ ] **[필수][INS-03] 모델 관리 아이콘을 눌러 팝업을 열었다.**
  - 기대 결과: SillyTavern 공식 팝업 안에 `v0.1.2` 배지와 `Vertex 모델 선택기 연결됨` 상태가 표시되고 팝업이 한 번만 열린다.
  - 결과/메모:
- [ ] **[필수][INS-04] 설치 전 Vertex 인증·프로젝트·리전 값이 바뀌지 않았다.**
  - 기대 결과: 확장은 기존 연결 정보를 읽어 별도 저장하거나 덮어쓰지 않는다.
  - 결과/메모:
- [ ] **[권장][INS-05] 브라우저 콘솔에 v0.1.2 초기화 완료 로그가 한 번 표시된다.**
  - 기대 결과: 관련 초기화 예외나 반복 오류가 없다.
  - 결과/메모:
- [ ] **[필수][INS-06] Extensions 설정 화면을 확인했다.**
  - 기대 결과: Custom Model Router의 긴 인라인 설정 블록이 없고 기존 Extensions 화면 높이를 늘리지 않는다.
  - 결과/메모:

## 2A. 모델 관리 팝업 UI/UX

- [ ] **[필수][UI-01] 모델 관리 아이콘을 마우스와 키보드로 각각 실행했다.**
  - 기대 결과: 클릭 또는 포커스 후 `Enter`/`Space`로 같은 팝업이 열리고 아이콘의 이름을 툴팁 또는 접근성 이름으로 확인할 수 있다.
  - 결과/메모:
- [ ] **[필수][UI-02] 팝업을 열고 닫는 동작을 3회 반복했다.**
  - 기대 결과: 매번 모델 관리 화면과 배경이 하나만 표시되고 이전 팝업이 겹쳐 남지 않는다.
  - 결과/메모:
- [ ] **[필수][UI-03] 팝업의 닫기 버튼으로 닫은 뒤 다시 열었다.**
  - 기대 결과: 팝업이 완전히 닫히고 모델 관리 아이콘으로 초점이 돌아오며 다시 정상적으로 열린다.
  - 결과/메모:
- [ ] **[필수][UI-04] 팝업을 연 뒤 `Escape` 키로 닫았다.**
  - 기대 결과: 팝업이 닫히고 모델 관리 아이콘으로 초점이 돌아간다.
  - 결과/메모:
- [ ] **[권장][UI-05] `Tab`과 `Shift+Tab`으로 팝업 안을 이동했다.**
  - 기대 결과: 입력란, 추가 버튼, 모델 작업 버튼, 닫기 동작에 키보드로 접근할 수 있고 초점이 보인다.
  - 결과/메모:
- [ ] **[필수][UI-06] 서로 다른 유효한 테스트 모델 ID를 20개 등록했다.**
  - 기대 결과: 모델 목록 영역 안에서만 세로 스크롤이 생기고 팝업과 API Connections 전체가 모델 수만큼 길어지지 않는다.
  - 정리: 실제 요청에 쓰지 않을 테스트 모델은 UI-10 확인 후 삭제한다.
  - 결과/메모:
- [ ] **[필수][UI-07] 20개 모델 목록의 처음·중간·끝 항목을 선택하거나 삭제했다.**
  - 기대 결과: 스크롤 위치와 관계없이 각 행의 모델 ID와 작업 아이콘이 대응하며 다른 행을 잘못 변경하지 않는다.
  - 결과/메모:
- [ ] **[권장][UI-08] 허용 최대 길이에 가까운 유효한 모델 ID를 등록했다.**
  - 기대 결과: 긴 ID가 줄바꿈되거나 안전하게 잘려 작업 아이콘과 팝업 경계를 침범하지 않는다.
  - 정리: 확인 뒤 긴 테스트 모델을 삭제한다.
  - 결과/메모:
- [ ] **[권장][UI-09] 브라우저 개발자 도구의 모바일 폭 또는 실제 좁은 화면에서 팝업을 열었다.**
  - 기대 결과: 팝업이 화면 너비를 벗어나지 않고 입력란, 추가 버튼, 목록, 작업 아이콘을 가로 스크롤 없이 사용할 수 있다.
  - 결과/메모:
- [ ] **[필수][UI-10] 팝업을 닫고 API Connections를 계속 사용했다.**
  - 기대 결과: 보이지 않는 배경이나 팝업 요소가 클릭·스크롤·키보드 입력을 막지 않는다.
  - 결과/메모:

## 3. 모델 등록과 입력 검증

- [ ] **[필수][REG-01] 모델 관리 팝업에서 유효한 사용자 Gemini 모델 ID를 등록했다.**
  - 기대 결과: 등록 목록과 Vertex 모델 선택기의 `사용자 지정 모델 · Custom Model Router` 그룹에 같은 ID가 한 번씩 표시된다.
  - 결과/메모:
- [ ] **[필수][REG-01A] 등록 직후 현재 Vertex 모델을 확인했다.**
  - 기대 결과: 등록만으로 사용자 모델이 자동 선택되지 않고 기존 모델이 유지된다.
  - 결과/메모:
- [ ] **[필수][REG-02] 같은 사용자 모델 ID를 다시 등록했다.**
  - 기대 결과: 중복 등록 오류가 표시되고 옵션이 늘어나지 않는다.
  - 결과/메모:
- [ ] **[필수][REG-03] SillyTavern 기본 목록에 이미 있는 모델 ID를 등록했다.**
  - 기대 결과: 기본 지원 모델이라는 오류가 표시되고 중복 옵션이 생기지 않는다.
  - 결과/메모:
- [ ] **[필수][REG-04] `gemini-`로 시작하지 않거나 공백·슬래시가 포함된 ID를 입력했다.**
  - 예시: `claude-test`, `gemini-test/model`, `gemini-test model`
  - 기대 결과: 입력 오류가 표시되고 모델이 저장되지 않는다.
  - 결과/메모:

## 4. 선택과 실제 Vertex 요청

- [ ] **[필수][REQ-01] 팝업의 등록 목록에서 사용자 모델의 선택 아이콘을 눌렀다.**
  - 기대 결과: `현재` 배지가 표시되고 Vertex 기본 모델 선택기의 값도 같은 모델 ID가 된다.
  - 결과/메모:
- [ ] **[필수][REQ-01A] 기본 모델로 바꾼 뒤 Vertex 선택기의 사용자 지정 그룹에서 사용자 모델을 직접 선택했다.**
  - 기대 결과: 등록 목록에도 `현재` 배지가 반영되고 사용자 선택이 기본 모델로 강제 복귀하지 않는다.
  - 결과/메모:
- [ ] **[필수][REQ-02] 일반 응답 방식으로 테스트 메시지를 보냈다.**
  - 기대 결과: 선택한 사용자 모델로 응답이 정상 완료된다.
  - 결과/메모:
- [ ] **[필수][REQ-03] 스트리밍 응답 방식으로 테스트 메시지를 보냈다.**
  - 기대 결과: 스트림이 정상 시작·종료되고 최종 메시지가 보존된다.
  - 결과/메모:
- [ ] **[선택][REQ-04] 가능한 환경에서 Network 요청이나 안전하게 정리한 서버 로그로 실제 모델 ID를 확인했다.**
  - 기대 결과: 요청된 모델 ID가 선택한 값과 정확히 일치하며 다른 모델로 자동 대체되지 않는다.
  - 주의: 증거에는 모델 필드만 남기고 인증 헤더, 토큰, 프로젝트 정보는 제거한다.
  - 결과/메모:
- [ ] **[필수][REQ-05] 요청 뒤에도 기존 인증 방식, 프로젝트, 리전 설정이 유지된다.**
  - 기대 결과: 확장 사용 전후의 연결 값이 같다.
  - 결과/메모:

## 5. 새로고침과 Connection Profile

- [ ] **[필수][PER-01] 사용자 모델을 선택한 상태에서 SillyTavern 페이지를 새로고침했다.**
  - 기대 결과: 사용자 옵션과 선택 상태가 자동 복원된다.
  - 결과/메모:
- [ ] **[필수][PER-02] 다른 API 제공업체로 전환한 뒤 Vertex AI로 돌아왔다.**
  - 기대 결과: 사용자 옵션이 중복 없이 남아 있고 이전 Vertex 사용자 모델 선택이 자동 복원된다.
  - 결과/메모:
- [ ] **[조건부][PER-03] 사용자 모델이 저장된 Connection Profile을 불러왔다.**
  - 기대 결과: 임의의 유사 모델이 아니라 정확한 사용자 모델 ID가 선택된다.
  - 결과/메모:
- [ ] **[조건부][PER-04] 다른 프로필로 전환한 뒤 사용자 모델 프로필로 돌아왔다.**
  - 기대 결과: 각 프로필 값에 맞춰 모델이 전환되고 사용자 옵션은 한 번만 표시된다.
  - 결과/메모:
- [ ] **[선택][PER-05] 현재 환경에서 모델 목록을 실제로 갱신하는 연결 설정이나 프로필 동작을 실행했다.**
  - 기대 결과: 선택기가 다시 그려지는 환경에서도 사용자 옵션과 change 동작이 복구된다.
  - 결과/메모:
- [ ] **[권장][PER-06] 새로고침과 API 제공업체 왕복 전환을 2~3회 반복했다.**
  - 기대 결과: 모델 관리 아이콘, 팝업, 사용자 지정 그룹, 모델 옵션이 중복 생성되지 않는다.
  - 결과/메모:
- [ ] **[필수][PER-07] 기본 Vertex 모델을 선택한 뒤 페이지를 새로고침했다.**
  - 기대 결과: 기본 모델 선택이 유지되고 등록된 사용자 모델이 강제로 다시 선택되지 않는다.
  - 결과/메모:

## 6. 삭제, 비활성화와 업데이트

- [ ] **[필수][LIFE-01] 사용자 모델을 다시 선택한 뒤 현재 선택 중인 모델을 삭제하려 했다.**
  - 기대 결과: 먼저 다른 Vertex 모델을 선택하라는 메시지가 표시되고 모델이 삭제되지 않는다.
  - 결과/메모:
- [ ] **[필수][LIFE-02] 기본 Vertex 모델을 선택한 뒤 사용자 모델을 삭제했다.**
  - 기대 결과: 등록 목록과 사용자 지정 optgroup에서 해당 모델이 제거되고 기본 옵션은 유지된다.
  - 결과/메모:
- [ ] **[권장][LIFE-03] 사용자 모델을 다시 등록·선택한 뒤 확장을 비활성화했다.**
  - 기대 결과: 열린 팝업과 모델 관리 아이콘, 사용자 지정 옵션이 제거되고 사용 가능한 기본 Vertex 모델로 전환되어 UI와 실제 설정이 어긋나지 않는다.
  - 결과/메모:
- [ ] **[권장][LIFE-04] 확장을 다시 활성화했다.**
  - 기대 결과: 모델 관리 아이콘은 하나만 다시 표시되고 등록 모델은 팝업에 복원되며, 사용자가 선택하기 전까지 기본 모델 선택은 유지된다.
  - 결과/메모:
- [ ] **[조건부][LIFE-05] v0.1.0 또는 v0.1.1이 설치된 환경에서 확장 관리자의 업데이트 기능으로 v0.1.2를 적용했다.**
  - 기대 결과: 팝업 버전 배지가 `v0.1.2`이고 기존 등록 모델 데이터가 보존되며 Extensions의 이전 인라인 블록은 남지 않는다.
  - 결과/메모:

## 7. 오류 처리와 선택 시험

- [ ] **[권장][ERR-01] 형식은 유효하지만 존재하지 않는 테스트 모델 ID로 요청했다.**
  - 기대 결과: 요청 실패가 명확히 표시되며 다른 모델로 조용히 대체되지 않는다. 실제 상태 코드는 인증 방식과 환경에 따라 다를 수 있다.
  - 정리: 시험 뒤 기본 Vertex 모델을 선택하고 존재하지 않는 모델을 삭제한다.
  - 결과/메모:
- [ ] **[선택][ERR-02] 현재 리전에서 사용할 수 없는 모델을 선택했다.**
  - 기대 결과: 권한·리전·모델 호환성을 확인할 수 있는 API 오류가 발생하며 기존 연결 정보는 유지된다.
  - 주의: 시험 전에 원래 리전과 모델을 기록하고 오류 확인 직후 복원한다.
  - 결과/메모:
- [ ] **[권장][ERR-03] 등록·전환·새로고침을 반복한 뒤 브라우저 콘솔을 확인했다.**
  - 기대 결과: 동일 UI나 옵션이 계속 늘어나지 않고, 반복 예외나 MutationObserver 루프가 없다.
  - 결과/메모:
- [ ] **[조건부][MODE-01] Express Mode 사용자로서 필수 요청 항목을 통과했다.**
  - 결과/메모:
- [ ] **[조건부][MODE-02] Full Mode(Service Account) 사용자로서 필수 요청 항목을 통과했다.**
  - 결과/메모:

## 8. 최종 판정

- [ ] **통과:** 모든 필수 항목이 기대 결과와 일치한다.
- [ ] **조건부 통과:** 핵심 요청은 성공했지만 권장 항목이나 특정 환경에 문제가 있다.
- [ ] **실패:** 필수 항목이 하나 이상 실패해 v0.1 안정화 수정이 필요하다.

실패가 있으면 [GitHub Issues](https://github.com/p16481012/Custom-Model-Router/issues)에 다음 양식으로 보고해 주세요.

```text
체크리스트 항목 ID:
SillyTavern 버전:
Custom Model Router 버전:
운영체제 / 브라우저:
Vertex 인증 방식: Express / Full
테스트 리전:
모델 ID:
스트리밍 사용 여부:
Connection Profile 사용 여부:
재현 순서:
기대 결과:
실제 결과:
민감정보를 제거한 오류 메시지 또는 로그:
```

진행 위치와 다음 개발 단계는 [ROADMAP.md](./ROADMAP.md)에서 확인할 수 있습니다.
