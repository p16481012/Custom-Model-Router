# 사용자 검증 체크리스트

대상 버전: **v0.4.0**

대상 기능: SillyTavern Chat Completion 연결 24개의 사용자 모델 등록·선택·복원, schema v1→v2 이관, API Connections 모델 관리 팝업

이 문서는 자동 테스트만으로 확인할 수 없는 실제 SillyTavern 화면과 제공업체 계정의 동작을 검증하기 위한 체크리스트입니다. 모든 24개 계정이 필요한 것은 아닙니다. 먼저 공통 필수 항목을 확인하고, 실제 사용하는 제공업체 행과 관련 특별 항목만 조건부로 확인해 주세요.

## 상태 기록 방법

- `[ ]`: 아직 확인하지 않음
- `[x]`: 기대 결과와 동일하게 통과
- `실패`: 항목 ID와 실제 결과를 문제 보고 양식에 기록
- `차단`: 계정 권한, 모델 제공 지역, 테스트 환경 등의 이유와 해제 조건을 기록
- `해당 없음`: 사용하지 않는 제공업체나 기능이라 확인 대상이 아님

`필수`는 공통 안정성 판단에 사용합니다. `조건부`는 해당 연결·기능을 사용하는 경우 필수입니다. `권장`과 `선택`은 가능한 환경에서 확인해 주세요.

```text
진행 상태: 시작 전 / 진행 중 / 완료 / 차단
공통 필수 통과: __ / __
확인한 제공업체: __ / 24
조건부 통과: __ / __
현재 확인 중인 항목 ID:
현재 차단 사항:
마지막 확인일:
```

## 테스트 환경

민감정보는 적지 않습니다. API 키, Service Account JSON, 전체 프로젝트 ID·Account ID, 프록시 자격 증명, 요청 Authorization 헤더는 반드시 제거합니다.

```text
SillyTavern 버전:
Custom Model Router 버전: v0.4.0
설치 방식: 신규 설치 / v0.1.x에서 업데이트
브라우저와 버전:
OS:
SillyTavern 실행 방식: 로컬 / 서버 / 모바일
확인할 제공업체:
endpoint 종류 또는 지역(비밀 아님):
Connection Profile 사용 여부:
스트리밍 사용 여부:
시험 모델 ID:
```

## 0. 사전 확인

- [ ] **[필수][PRE-01] SillyTavern이 1.18.0 이상인지 확인했다.**
  - 기대 결과: 버전을 기록했고 API Connections의 Chat Completion 화면을 열 수 있다.

- [ ] **[필수][PRE-02] 시험할 제공업체의 기본 모델로 메시지 생성이 성공한다.**
  - 기대 결과: 확장 모델을 적용하기 전 인증·endpoint·리전이 정상임을 분리해 확인했다.

- [ ] **[필수][PRE-03] 제공업체 공식 문서나 계정 콘솔에서 정확한 시험 모델 ID를 확인했다.**
  - 기대 결과: 표시 이름이 아닌 API 요청용 ID를 준비했다.

- [ ] **[필수][PRE-04] 시험 모델이 현재 기본 선택기에 이미 있는지 확인했다.**
  - 기대 결과: 이미 기본 옵션이면 중복 방지 시험으로 분류하고, 신규 등록 시험에는 목록에 없는 ID를 사용한다.

- [ ] **[필수][PRE-05] 업데이트 또는 대량 등록 전에 SillyTavern 설정을 백업했다.**
  - 기대 결과: 문제가 생겨도 기존 Connection Profile과 확장 설정을 복구할 수 있다.

- [ ] **[권장][PRE-06] 개발자 도구 Console과 Network 탭을 열 수 있다.**
  - 기대 결과: 실패 시 토큰을 가린 오류와 model 값만 확인할 준비가 됐다.

## 1. 설치와 v1→v2 자동 이관

- [ ] **[필수][MIG-01] 확장 업데이트 뒤 API Connections에 사용자 모델 관리 아이콘이 한 번만 표시된다.**
  - 기대 결과: 아이콘이 Connection Profile 도구행에 있고 다른 컨트롤을 가리지 않는다.

- [ ] **[필수][MIG-02] 관리 팝업에 `v0.4.0` 배지와 제공업체 선택기가 표시된다.**
  - 기대 결과: Extensions 설정에 긴 인라인 패널이 생기지 않고 공식 팝업이 하나만 열린다.

- [ ] **[조건부][MIG-03] v0.1.x에 등록했던 Vertex 모델이 업데이트 뒤 Google Vertex AI 목록에 남아 있다.**
  - 기대 결과: 기존 ID가 빠지거나 다른 제공업체로 이동하지 않는다.

- [ ] **[조건부][MIG-04] v0.1.x에서 선택했던 Vertex 사용자 모델 상태가 v0.4.0에서 복원된다.**
  - 기대 결과: schema v1의 단일 선택값이 `vertexai` 선택 상태로 이관된다.

- [ ] **[조건부][MIG-05] v0.1.x에 중복 또는 손상된 저장값이 있어도 팝업을 열 수 있다.**
  - 기대 결과: 유효한 Vertex 모델은 한 번만 남고 잘못된 레코드 때문에 확장이 중단되지 않는다.

- [ ] **[필수][MIG-06] 신규 설치에서도 빈 Registry로 정상 시작한다.**
  - 기대 결과: 등록 모델 0개 상태에서 오류 없이 제공업체를 전환할 수 있다.

## 2. 공통 UI와 등록

- [ ] **[필수][UI-01] 제공업체 선택기에 모델 개발사 API, 라우터·호스팅, 사용자 지정 연결 그룹이 표시된다.**
  - 기대 결과: 총 24개 연결을 찾을 수 있다.

- [ ] **[필수][UI-02] Z.AI (GLM), DeepSeek, Moonshot AI (Kimi), MiniMax, SiliconFlow를 각각 찾을 수 있다.**
  - 기대 결과: 이 모델 업체들이 Custom 하나로 뭉치지 않고 전용 연결로 제공된다.

- [ ] **[필수][UI-03] 제공업체를 바꾸면 모델 ID 라벨, 예시, 허용 문자 안내와 목록 제목이 함께 바뀐다.**
  - 기대 결과: 직전 제공업체의 안내가 남지 않는다.

- [ ] **[필수][UI-04] 현재 활성화하지 않은 제공업체에도 모델을 미리 등록할 수 있다.**
  - 기대 결과: 등록은 성공하지만 선택 버튼은 해당 연결을 먼저 활성화하라고 안내한다.

- [ ] **[필수][UI-05] 현재 활성화한 제공업체에서는 등록 모델의 선택 버튼을 사용할 수 있다.**
  - 기대 결과: 모델 컨트롤 감지 상태가 정상으로 표시되고 버튼이 활성화된다.

- [ ] **[필수][UI-06] 서로 다른 제공업체에 같은 모델 ID를 각각 등록했다.**
  - 기대 결과: `provider + model ID`가 복합키이므로 두 등록이 모두 유지된다.

- [ ] **[필수][UI-07] 같은 제공업체에 같은 모델 ID를 다시 등록했다.**
  - 기대 결과: 중복 안내가 나오고 항목은 한 개만 남는다.

- [ ] **[필수][UI-08] URL, 공백, 쿼리 문자 또는 HTML이 포함된 값을 등록했다.**
  - 기대 결과: 입력이 거부되고 DOM이나 저장 설정에 삽입되지 않는다.

- [ ] **[필수][UI-09] Google AI Studio 또는 Vertex AI에 `/`가 포함된 모델 ID를 등록했다.**
  - 기대 결과: URL path segment 규칙에 따라 거부된다.

- [ ] **[조건부][UI-10] OpenRouter·SiliconFlow·Workers AI·Fireworks 또는 Custom에 공식 계층형 ID를 등록했다.**
  - 기대 결과: `/`, `:`, `+`, `@` 중 해당 ID에 필요한 문자가 보존된다.

- [ ] **[필수][UI-11] 한 제공업체에 모델 20개를 등록하고 처음·중간·끝 항목을 조작했다.**
  - 기대 결과: 목록 영역만 스크롤되고 추가 폼과 제공업체 선택기는 화면에서 밀려나지 않는다.

- [ ] **[권장][UI-12] 좁은 화면에서 긴 모델 ID와 99개 초과 총 등록 수를 확인했다.**
  - 기대 결과: ID·버튼이 겹치지 않고 런처 배지는 `99+`로 표시된다.

- [ ] **[필수][UI-13] 팝업을 `Escape`로 닫고 다시 열었다.**
  - 기대 결과: 닫힌 뒤 관리 아이콘으로 초점이 돌아가며 중복 팝업이나 이벤트가 생기지 않는다.

## 3. 선택·복원·삭제

- [ ] **[필수][SEL-01] 팝업 목록에서 등록 모델을 적용했다.**
  - 기대 결과: 해당 기본 모델 컨트롤과 SillyTavern 설정의 model 값이 같은 ID가 된다.

- [ ] **[필수][SEL-02] 기본 모델 선택기의 사용자 지정 그룹에서 등록 모델을 직접 선택했다.**
  - 기대 결과: 팝업을 다시 열면 같은 항목에 현재 상태가 표시된다.

- [ ] **[필수][SEL-03] 다른 제공업체로 전환했다가 돌아왔다.**
  - 기대 결과: 각 제공업체의 등록 목록과 선택 상태가 서로 섞이지 않는다.

- [ ] **[필수][SEL-04] 사용자 모델 선택 상태에서 페이지를 새로고침했다.**
  - 기대 결과: 기본 선택기가 만들어진 뒤 사용자 옵션과 선택 상태가 한 번만 복원된다.

- [ ] **[조건부][SEL-05] Connection Profile을 다른 프로필로 전환한 뒤 원래 프로필로 돌아왔다.**
  - 기대 결과: 프로필 전환 뒤 선택기가 재생성되어도 사용자 옵션이 중복 없이 복원된다.

- [ ] **[조건부][SEL-06] 원격 모델 목록을 새로 불러오는 제공업체에 다시 연결했다.**
  - 기대 결과: 코어가 selector 내용을 교체한 뒤 확장 그룹과 저장 선택이 복원된다.

- [ ] **[필수][SEL-07] SillyTavern 기본 목록에 동일한 ID가 있는 상태를 만들었다.**
  - 기대 결과: 확장 그룹에 중복 옵션을 만들지 않고 기본 옵션을 사용한다.

- [ ] **[필수][SEL-08] 현재 선택 중인 사용자 전용 모델의 삭제를 시도했다.**
  - 기대 결과: 선택형 연결에서는 먼저 다른 모델을 선택하라는 안내가 나오고 삭제되지 않는다.

- [ ] **[필수][SEL-09] 기본 모델을 선택한 뒤 사용자 모델을 삭제했다.**
  - 기대 결과: 해당 제공업체 등록만 삭제되고 다른 제공업체의 같은 ID와 기본 옵션은 남는다.

- [ ] **[조건부][SEL-10] Custom OpenAI-compatible 모델을 적용했다.**
  - 기대 결과: `#custom_model_id` 입력값이 바뀌고 SillyTavern의 native input 흐름이 실행된다. Registry 등록만 삭제하면 현재 입력값은 유지된다고 안내된다.

## 4. 실제 요청 공통 항목

- [ ] **[조건부][REQ-01] 등록 모델로 일반 텍스트 메시지를 생성했다.**
  - 기대 결과: 응답이 완료되고 현재 선택이 임의의 기본 모델로 바뀌지 않는다.

- [ ] **[조건부][REQ-02] 민감정보를 가린 요청 정보에서 model 값이 등록 ID인지 확인했다.**
  - 기대 결과: API 키·Authorization을 기록하지 않고 model 값만 일치함을 확인한다.

- [ ] **[조건부][REQ-03] 같은 모델로 스트리밍 메시지를 생성했다.**
  - 기대 결과: 스트림이 정상 종료되고 중복 토큰·무한 로딩·선택 초기화가 없다.

- [ ] **[조건부][REQ-04] 대화를 한 번 이어서 생성했다.**
  - 기대 결과: 두 번째 요청에도 같은 model 값이 사용되고 문맥이 정상 전달된다.

- [ ] **[조건부][REQ-05] 존재하지 않거나 권한 없는 시험 ID로 오류 동작을 확인했다.**
  - 기대 결과: 제공업체 오류가 표시되고 확장이 조용히 다른 모델로 대체하지 않는다.

- [ ] **[선택][REQ-06] tool 호출, thinking 또는 멀티모달 기능을 시험했다.**
  - 기대 결과: 결과를 별도로 기록한다. 실패해도 ID-only 텍스트 지원까지 실패로 확장 해석하지 않는다.

## 5. 제공업체 조건부 검증 매트릭스

각 행은 그 연결을 실제로 사용할 때만 조건부 필수입니다. 시험 모델 ID는 현재 공식 문서에서 확인한 값을 사용하고, 아래 형식 예시는 존재나 제공 여부를 보장하지 않습니다.

| 항목 ID | 연결 | 준비 조건·ID 형식 | 등록·적용 | 일반 | 스트리밍 | 새로고침·Profile |
|---|---|---|---|---|---|---|
| **[조건부][PV-OPENAI]** | OpenAI | 현재 API와 호환되는 `gpt-...`, `o...` 등 | [ ] | [ ] | [ ] | [ ] |
| **[조건부][PV-CLAUDE]** | Anthropic | 현재 Messages API와 호환되는 `claude-...` | [ ] | [ ] | [ ] | [ ] |
| **[조건부][PV-AI21]** | AI21 | 현재 Jamba Chat 규격과 호환되는 ID | [ ] | [ ] | [ ] | [ ] |
| **[조건부][PV-COHERE]** | Cohere | 현재 Cohere Chat 규격과 호환되는 ID | [ ] | [ ] | [ ] | [ ] |
| **[조건부][PV-DEEPSEEK]** | DeepSeek | DeepSeek 전용 요청 처리와 호환되는 ID | [ ] | [ ] | [ ] | [ ] |
| **[조건부][PV-MAKERSUITE]** | Google AI Studio | URL 한 구간인 Gemini·Gemma 계열 ID | [ ] | [ ] | [ ] | [ ] |
| **[조건부][PV-VERTEX]** | Google Vertex AI | 프로젝트·리전에서 이용 가능한 `generateContent` 호환 ID | [ ] | [ ] | [ ] | [ ] |
| **[조건부][PV-GROQ]** | Groq | 연결 완료 후 공식 chat model ID | [ ] | [ ] | [ ] | [ ] |
| **[조건부][PV-MISTRAL]** | Mistral AI | chat capability가 있는 모델 ID | [ ] | [ ] | [ ] | [ ] |
| **[조건부][PV-MINIMAX]** | MiniMax | 선택한 Global/CN endpoint에서 가능한 ID | [ ] | [ ] | [ ] | [ ] |
| **[조건부][PV-MOONSHOT]** | Moonshot AI (Kimi) | 현재 Kimi Chat 규격과 호환되는 ID | [ ] | [ ] | [ ] | [ ] |
| **[조건부][PV-PERPLEXITY]** | Perplexity | 현재 Chat Completion과 호환되는 ID | [ ] | [ ] | [ ] | [ ] |
| **[조건부][PV-XAI]** | xAI | 현재 xAI Chat Completion과 호환되는 `grok-...` | [ ] | [ ] | [ ] | [ ] |
| **[조건부][PV-ZAI]** | Z.AI (GLM) | 선택한 Common/Coding endpoint의 `glm-...` | [ ] | [ ] | [ ] | [ ] |
| **[조건부][PV-AIMLAPI]** | AI/ML API | chat-completion 유형으로 제공되는 ID | [ ] | [ ] | [ ] | [ ] |
| **[조건부][PV-CHUTES]** | Chutes | `/v1/chat/completions` 호환 카탈로그 ID | [ ] | [ ] | [ ] | [ ] |
| **[조건부][PV-WORKERS]** | Cloudflare Workers AI | Account ID 설정 후 `@cf/vendor/model` 형식 | [ ] | [ ] | [ ] | [ ] |
| **[조건부][PV-ELECTRONHUB]** | ElectronHub | Chat Completion endpoint가 있는 ID | [ ] | [ ] | [ ] | [ ] |
| **[조건부][PV-FIREWORKS]** | Fireworks AI | chat 지원 `accounts/.../models/...` ID | [ ] | [ ] | [ ] | [ ] |
| **[조건부][PV-NANOGPT]** | NanoGPT | 현재 catalog의 Chat Completion ID | [ ] | [ ] | [ ] | [ ] |
| **[조건부][PV-OPENROUTER]** | OpenRouter | `vendor/model`과 필요한 variant를 포함한 ID | [ ] | [ ] | [ ] | [ ] |
| **[조건부][PV-POLLINATIONS]** | Pollinations | 현재 Chat Completion model ID | [ ] | [ ] | [ ] | [ ] |
| **[조건부][PV-SILICONFLOW]** | SiliconFlow | Global/CN endpoint의 `vendor/model` ID | [ ] | [ ] | [ ] | [ ] |
| **[조건부][PV-CUSTOM]** | Custom OpenAI-compatible | 기존 Custom endpoint를 설정하고 그 서버의 ID 사용 | [ ] | [ ] | [ ] | [ ] |

매트릭스에서 실패하면 공통 `REQ-*` 결과도 함께 적어 주세요. 원격 목록에 없는 사용자 모델은 context·가격·멀티모달 metadata가 없어 텍스트 생성은 성공해도 자동 capability 판단이 다를 수 있습니다.

## 6. Z.AI (GLM) 특별 확인

사용자가 요청한 GLM 계열은 `Custom`이 아니라 SillyTavern의 Z.AI 전용 연결을 우선 사용합니다.

- [ ] **[조건부][GLM-01] API Connections의 현재 source를 Z.AI로 선택했다.**
  - 기대 결과: 팝업에 `Z.AI (GLM) 모델 선택기 감지됨 · 현재 연결` 상태가 표시된다.

- [ ] **[조건부][GLM-02] 공식 문서에서 확인한 `glm-...` ID를 Z.AI (GLM)에 등록했다.**
  - 기대 결과: Z.AI 모델 선택기의 사용자 지정 그룹에 같은 ID가 한 번 표시된다.

- [ ] **[조건부][GLM-03] 등록 모델을 적용하기 전후의 Common 또는 Coding endpoint 선택을 비교했다.**
  - 기대 결과: endpoint 종류, API 키와 다른 연결 설정이 바뀌지 않는다.

- [ ] **[조건부][GLM-04] GLM 사용자 모델로 일반 응답을 생성했다.**
  - 기대 결과: Z.AI 응답이 완료되고 요청 model 값이 등록 ID와 일치한다.

- [ ] **[조건부][GLM-05] GLM 사용자 모델로 스트리밍 응답을 생성했다.**
  - 기대 결과: 스트림이 정상 종료되고 선택값이 유지된다.

- [ ] **[조건부][GLM-06] 기본 GLM 모델과 사용자 GLM 모델을 왕복 전환했다.**
  - 기대 결과: 각각의 선택이 native change 흐름에 반영되고 중복 옵션이 생기지 않는다.

- [ ] **[조건부][GLM-07] 새로고침 또는 Connection Profile 전환 뒤 Z.AI로 돌아왔다.**
  - 기대 결과: 사용자 GLM 옵션과 저장 선택 상태가 복원된다.

- [ ] **[권장][GLM-08] Z.AI가 모델명에 따라 제한하는 sampling·stop 파라미터가 오류를 내는지 확인했다.**
  - 기대 결과: 결과를 기록하고, 모델 ID 등록 성공과 요청 규격 호환성을 구분한다.

## 7. 제외 항목과 수명주기

- [ ] **[필수][EXC-01] 제공업체 선택기에 Azure OpenAI가 등록 대상으로 나오지 않는다.**
  - 기대 결과: Azure OpenAI는 deployment name이 요청 대상을 결정하므로 모델 ID-only 기능에 포함되지 않는다.

- [ ] **[필수][EXC-02] 제공업체 선택기에 CometAPI가 등록 대상으로 나오지 않는다.**
  - 기대 결과: SillyTavern 1.18.0 코어에서 비활성화된 연결을 지원한다고 표시하지 않는다.

- [ ] **[필수][LIFE-01] 사용자 모델을 선택한 상태에서 확장을 비활성화했다.**
  - 기대 결과: 열린 팝업, 관리 아이콘, 확장 optgroup과 이벤트가 정리되고 선택형 연결은 가능한 기본 fallback으로 이동한다.

- [ ] **[필수][LIFE-02] 확장을 다시 활성화했다.**
  - 기대 결과: 저장 Registry가 복원되고 런처·옵션·이벤트가 각각 한 개만 생긴다.

- [ ] **[필수][LIFE-03] 확장을 여러 번 열고 닫거나 source를 빠르게 전환했다.**
  - 기대 결과: MutationObserver 반복, UI 멈춤, 중복 저장 호출이 발생하지 않는다.

- [ ] **[조건부][LIFE-04] 원격 모델 목록 로딩 중 확장을 비활성화했다.**
  - 기대 결과: 지연 작업이 끝난 뒤 런처나 사용자 옵션이 되살아나지 않는다.

## 8. 설정 보존과 보안

- [ ] **[필수][SEC-01] 등록·선택·삭제 전후 API 키와 인증 방식이 바뀌지 않았다.**
  - 기대 결과: 확장은 인증 값을 읽거나 자체 저장하지 않는다.

- [ ] **[필수][SEC-02] endpoint URL, 프로젝트·Account ID, 리전과 필터 설정이 바뀌지 않았다.**
  - 기대 결과: 모델 값 외 연결 설정이 그대로 유지된다.

- [ ] **[권장][SEC-03] 내보낸 확장 설정에 민감정보가 새로 복제되지 않았는지 확인했다.**
  - 기대 결과: `customModelRouter`에는 schema, 모델 ID, provider, protocol, enabled, 선택 상태만 있다.

- [ ] **[필수][SEC-04] 오류 보고 전에 Console·Network 자료에서 비밀값을 제거했다.**
  - 기대 결과: Authorization, API 키, Service Account JSON, 전체 프로젝트·Account ID가 포함되지 않는다.

## 9. 공개 Registry API

- [ ] **[필수][API-01] 브라우저 Console에서 `CustomModelRouter.apiVersion`을 확인했다.**
  - 기대 결과: `1.1.0`이며 `CustomModelRouter.isCompatible('1.1.0')`가 `true`다.

- [ ] **[필수][API-02] `CustomModelRouter.getSnapshot()`을 호출했다.**
  - 기대 결과: 등록 모델과 `selectedModels`가 보이고 API 키·endpoint·프로젝트·리전은 포함되지 않는다.

- [ ] **[권장][API-03] `CustomModelRouter.subscribe('registry:changed', console.log)`로 구독한 뒤 모델을 하나 등록했다.**
  - 기대 결과: revision과 불변 snapshot이 포함된 이벤트가 한 번의 변경 단위로 전달된다.

- [ ] **[필수][API-04] 확장을 비활성화한 뒤 전역 API가 제거되는지 확인했다.**
  - 기대 결과: `globalThis.CustomModelRouter`가 제거되고 재활성화하면 새 API 인스턴스가 한 개만 설치된다.

## 10. 용도별 보조 모델 라우팅

- [ ] **[필수][ROUTE-01] 보조 기능별 모델 라우팅을 펼치고 번역 용도를 선택했다.**
  - 기대 결과: 등록 모델과 같은 제공업체의 Connection Profile만 선택할 수 있다.

- [ ] **[필수][ROUTE-02] 번역 용도에 모델과 프로필을 저장했다.**
  - 기대 결과: 별칭이 생기지 않고 제공업체·실제 모델 ID·프로필 ID가 표시된다.

- [ ] **[필수][ROUTE-03] 경로의 테스트 요청을 실행했다.**
  - 기대 결과: 보조 응답이 표시되고 메인 Chat Completion source와 모델 선택은 전후 동일하다.

- [ ] **[필수][ROUTE-04] 다른 제공업체의 프로필을 고를 수 없는지 확인했다.**
  - 기대 결과: 목록에서 제외되며 API로 잘못된 조합을 넣어도 provider mismatch 오류가 난다.

- [ ] **[필수][ROUTE-05] 경로에 사용한 모델 등록 또는 Connection Profile을 제거한 뒤 테스트했다.**
  - 기대 결과: 삭제된 대상이 명시된 오류가 나고 메인 모델이나 다른 모델로 대체되지 않는다.

- [ ] **[권장][ROUTE-06] 요약·검색 보조·이미지 설명 용도에 서로 다른 모델을 저장했다.**
  - 기대 결과: 각 용도가 `(provider, model ID)` 복합키로 독립 저장된다.

- [ ] **[필수][ROUTE-07] Console에서 `CustomModelRouter.routing.isCompatible('1.0.0')`을 확인했다.**
  - 기대 결과: `true`이며 `capabilities.silentFallback`과 `routerMutatesMainChatModel`이 모두 `false`다.

- [ ] **[필수][ROUTE-08] 번역 경로를 해제했다.**
  - 기대 결과: 등록 모델과 Connection Profile 자체는 삭제되지 않고 해당 용도 매핑만 사라진다.

## 결과 제출 양식

```text
제목: [v0.4.0][제공업체][항목 ID] 짧은 증상

SillyTavern 버전:
Custom Model Router 버전: v0.4.0
OS / 브라우저:
설치 방식: 신규 / v0.1.x에서 업데이트
제공업체:
endpoint 종류 또는 지역(민감정보 제외):
Connection Profile 사용 여부:
스트리밍 여부:
실패 항목 ID:
시험 모델 ID:

재현 순서:
1.
2.
3.

기대 결과:
실제 결과:
오류 메시지(토큰·키·계정 ID 제거):
요청의 model 값만 확인한 결과:
스크린샷(민감정보 가림):
재현 빈도:
기본 모델 요청 성공 여부:
```

한 항목이 실패해도 나머지를 모두 중단할 필요는 없습니다. 데이터 손실, 인증 설정 변경, 무한 요청, 화면 멈춤이 발생하면 즉시 시험을 멈추고 `치명`으로 보고해 주세요. 일반 생성 실패나 선택 복원 실패는 해당 항목 ID와 제공업체를 기록해 `v0.2.2+` 보완 범위를 결정하는 데 사용합니다.
